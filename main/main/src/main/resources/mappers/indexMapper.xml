<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "//mybatis.org/DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd" >

<mapper namespace="com.video.main.repository.indexDAO">

    <select id="indexGetName" resultType="String">
        SELECT distinct(first_name) FROM sakila.actor where first_name = 'PENELOPE'
    </select>
    
</mapper>